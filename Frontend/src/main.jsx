import React from 'react';
import ReactDOM from 'react-dom/client';
import CrowdTrendDashboard from './CrowdTrendDashboard';

const root = ReactDOM.createRoot(document.getElementById('root'));
root.render(
  <React.StrictMode>
    <CrowdTrendDashboard />
  </React.StrictMode>
);
